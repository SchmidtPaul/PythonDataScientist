staticryptR::staticryptr(
  files = "docs/",        # Changed from _output to docs
  directory = ".",
  password = "pw",
  short = TRUE,
  recursive = TRUE,
  template_title = "Your Website Title",
  template_instructions = "Please enter the password to access this site",
  template_button = "Access"
)